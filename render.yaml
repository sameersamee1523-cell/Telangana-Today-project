services:
  - type: web
    name: pipeline-server
    env: node
    region: oregon
    plan: free
    buildCommand: npm install --prefix backend
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      # ── TiDB MySQL ──────────────────────────────────
      - key: DB_HOST
        value: 8.222.211.151
      - key: DB_PORT
        value: "4000"
      - key: DB_USER
        value: ox4YpiyrWbH1THa.root
      - key: DB_PASSWORD
        value: iu3NMBIP2jGfcIDs
      - key: DB_NAME
        value: pipeline_db
      - key: DB_SSL
        value: "true"
      # ── Auth ─────────────────────────────────────────
      - key: JWT_SECRET
        value: telangana_today_super_secret_jwt_key_2024_pipeline
      - key: JWT_EXPIRES_IN
        value: "7d"
      # ── Frontend URL ──────────────────────────────────
      - key: CLIENT_URL
        value: "https://telangana-today.vercel.app"
